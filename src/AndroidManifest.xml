<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sibyl">
    <application android:icon="@drawable/icon">
        <service android:name=".Sibylservice" android:process=":remote" />

        <activity android:label="@string/app_name" android:theme="@android:style/Theme.Dark" android:multiprocess="false" android:name=".ui.PlayerUI">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
         
        <activity android:name=".Test" android:label="@string/app_name">
        
        </activity>
        <activity android:name=".ui.PlayListUI" android:label="@string/app_name">
        
        </activity>
    </application>
</manifest> 